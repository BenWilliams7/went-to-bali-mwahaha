$("#<%= @item.id %>").remove();
$('.cart-count').empty().html('<%= j link_to "Cart | Total items: #{ cart_items }", cart_path %>');
$(".total").empty().append("<%= number_to_currency current_order.total_price %>");
